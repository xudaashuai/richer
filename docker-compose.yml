version: '3'
services:
  web:
    build: .
    ports:
      - '8000:80'
  backend:
    build:
      context: .
      dockerfile: ServerDockerfile
    ports:
      - '8001:8000'
  nginx:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - '9000:80'
